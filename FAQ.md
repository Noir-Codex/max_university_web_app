# ❓ FAQ - Часто задаваемые вопросы

Ответы на наиболее распространённые вопросы о MAX Attendance Tracker.

---

## 📑 Содержание

1. [Общие вопросы](#-общие-вопросы)
2. [Установка и настройка](#-установка-и-настройка)
3. [Интеграция с MAX](#-интеграция-с-max)
4. [Аутентификация и безопасность](#-аутентификация-и-безопасность)
5. [Функциональность](#-функциональность)
6. [Производительность](#-производительность)
7. [Troubleshooting](#-troubleshooting)
8. [Развёртывание](#-развёртывание)

---

## 🌐 Общие вопросы

### Что такое MAX Attendance Tracker?

MAX Attendance Tracker - это WebApp приложение для платформы MAX, предназначенное для учёта посещаемости в образовательных учреждениях. Позволяет преподавателям отмечать студентов, администраторам управлять расписанием, а студентам отслеживать свою посещаемость.

### Какие платформы поддерживаются?

Приложение работает на всех платформах, где доступен MAX:
- ✅ iOS (iPhone, iPad)
- ✅ Android
- ✅ MAX Web
- ✅ MAX Desktop (Windows, macOS, Linux)

### Нужен ли интернет для работы?

Для полноценной работы требуется интернет-соединение. Однако базовый просмотр данных доступен после первоначальной загрузки благодаря кэшированию.

### Какая лицензия у проекта?

Проект распространяется под лицензией ISC. См. файл [LICENSE](LICENSE) для деталей.

### Можно ли использовать для коммерческих целей?

Да, проект можно использовать в коммерческих целях в соответствии с лицензией ISC.

---

## 🔧 Установка и настройка

### Как установить зависимости?

**Для фронтенда:**
```bash
cd max-webapp
npm install
```

**Для бэкенда:**
```bash
cd server
npm install
```

### Ошибка "Cannot find module"

**Решение:**
```bash
# Очистите кэш и переустановите зависимости
rm -rf node_modules package-lock.json
npm install

# Убедитесь что версия Node.js >= 16.x
node --version
```

### Как настроить PostgreSQL?

**Локальная установка:**
```bash
# macOS
brew install postgresql@14
brew services start postgresql@14

# Создайте базу данных
createdb max_attendance
```

**Удалённая база данных:**
Настройте параметры подключения в `server/.env`:
```env
DB_HOST=your-db-host.com
DB_USER=your_user
DB_NAME=max_attendance
DB_PASSWORD=your_password
DB_PORT=5432
```

### Ошибка "SSL connection required"

Если ваша PostgreSQL требует SSL:

```bash
# Скачайте SSL сертификат
mkdir -p ~/.cloud-certs
wget https://your-provider.com/ca-certificate.crt -O ~/.cloud-certs/root.crt

# Укажите путь в .env
DB_SSL_CERT_PATH=~/.cloud-certs/root.crt
```

### Как инициализировать базу данных?

```bash
cd server
npm run init-db
```

Это создаст все необходимые таблицы.

### Ошибка "port already in use"

**Решение:**
```bash
# Найдите процесс на порту
lsof -i :3001

# Убейте процесс
kill -9 <PID>

# Или используйте другой порт в .env
PORT=3002
```

---

## 📱 Интеграция с MAX

### Как создать MAX бота?

1. Откройте MAX и найдите **@BotFather**
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Сохраните Bot Token

Подробнее: [`MAX_INTEGRATION_GUIDE.md`](MAX_INTEGRATION_GUIDE.md:1)

### Как настроить WebApp?

В @BotFather:
```
/mybots
Выберите вашего бота
Bot Settings → Menu Button → Configure menu button
Введите URL: https://your-domain.com
```

### WebApp не открывается в MAX

**Возможные причины:**

1. **URL не использует HTTPS:**
   - MAX требует HTTPS для WebApp
   - Используйте SSL сертификат

2. **Неправильный URL:**
   - Проверьте что URL доступен
   - Проверьте CORS настройки

3. **Проблемы с сертификатом:**
   - Проверьте валидность SSL сертификата
   - Используйте Let's Encrypt для бесплатного SSL

### Ошибка "Invalid initData"

**Причины:**
- Неправильный Bot Token
- Неверная валидация на сервере
- Истекший initData

**Решение:**
```javascript
// Проверьте Bot Token в .env
TELEGRAM_BOT_TOKEN=correct-token-here

// Убедитесь что валидация работает
// См. MAX_INTEGRATION_GUIDE.md раздел 4.2
```

### Как тестировать локально?

Используйте **ngrok** для туннеля:
```bash
# Установите ngrok
brew install ngrok  # macOS

# Запустите туннель
ngrok http 3000

# Используйте HTTPS URL в @BotFather
https://abc123.ngrok.io
```

---

## 🔐 Аутентификация и безопасность

### Как генерируется JWT токен?

При успешном логине сервер:
1. Валидирует MAX initData
2. Создаёт/находит пользователя в БД
3. Генерирует JWT токен с user info
4. Возвращает токен клиенту

Токен хранится в `localStorage` и используется для всех API запросов.

### Безопасно ли хранить токен в localStorage?

Для MAX WebApp это приемлемо, так как:
- WebApp работает внутри MAX
- Нет риска XSS от сторонних скриптов
- Альтернативы (cookies) сложнее в WebApp контексте

**Меры безопасности:**
- Короткое время жизни токена (7 дней)
- Валидация на сервере
- HTTPS везде

### Как сменить пароль?

Если используется password-based auth (не MAX):
```
POST /api/users/me/password
{
  "old_password": "current_password",
  "new_password": "new_secure_password"
}
```

### "Access denied" при попытке доступа

**Причины:**
- Недостаточно прав (проверьте роль пользователя)
- Истёк JWT токен
- Не авторизован

**Решение:**
```javascript
// Проверьте роль пользователя
console.log(user.role); // должен быть 'teacher' или 'admin'

// Обновите токен
localStorage.removeItem('auth_token');
// Войдите снова
```

### Как изменить роль пользователя?

Только администратор может менять роли:
```
PUT /api/users/:id
{
  "role": "teacher"
}
```

---

## 🎯 Функциональность

### Как отметить посещаемость?

**Для преподавателей:**
1. Откройте "Расписание"
2. Выберите занятие
3. Нажмите "Отметить посещаемость"
4. Отметьте каждого студента
5. Нажмите "Сохранить"

**Через API:**
```javascript
POST /api/attendance/bulk
{
  "lesson_id": 1,
  "date": "2024-01-25",
  "attendance": [
    { "student_id": 5, "status": "present" },
    { "student_id": 6, "status": "absent" }
  ]
}
```

### Можно ли изменить посещаемость задним числом?

Да, если вы преподаватель или администратор:
```
PUT /api/attendance/:id
{
  "status": "excused",
  "notes": "Справка от врача"
}
```

### Как экспортировать отчёт?

```javascript
// CSV формат
GET /api/reports/export?
  group_id=1&
  date_from=2024-01-01&
  date_to=2024-01-31&
  format=csv

// Excel формат
GET /api/reports/export?
  group_id=1&
  date_from=2024-01-01&
  date_to=2024-01-31&
  format=xlsx
```

### Как импортировать расписание?

**Через UI:**
1. Панель администратора → Импорт
2. Загрузите Excel файл
3. Проверьте preview
4. Подтвердите импорт

**Через API:**
```javascript
POST /api/import/schedule
FormData: {
  file: excel_file,
  mode: "append" // или "replace"
}
```

### Формат Excel для импорта

| Предмет | Группа | Преподаватель | День | Время начала | Время конца | Аудитория | Тип |
|---------|--------|---------------|------|--------------|-------------|-----------|-----|
| Математика | ИС-21 | Иванов И.И. | Понедельник | 09:00 | 10:30 | 301 | Лекция |

### Как добавить нового студента?

**Через UI:**
Панель администратора → Пользователи → Добавить пользователя

**Через API:**
```javascript
POST /api/users
{
  "telegram_id": 123456789,
  "first_name": "Alice",
  "last_name": "Johnson",
  "email": "alice@example.com",
  "role": "student"
}

// Добавьте студента в группу
POST /api/groups/:groupId/students
{
  "student_id": 5
}
```

### Статусы посещаемости

| Статус | Значение | Цвет |
|--------|----------|------|
| `present` | Присутствовал | ✅ Зелёный |
| `absent` | Отсутствовал | ❌ Красный |
| `late` | Опоздал | ⚠️ Жёлтый |
| `excused` | Уважительная причина | 📋 Синий |

---

## ⚡ Производительность

### Приложение загружается медленно

**Решения:**

1. **Оптимизируйте изображения:**
```bash
# Используйте WebP формат
# Lazy loading для изображений
<img loading="lazy" src="image.webp" />
```

2. **Code splitting:**
```javascript
// Lazy load компонентов
const AdminDashboard = lazy(() => import('./AdminDashboard'));
```

3. **Кэширование:**
```javascript
// TanStack Query автоматически кэширует запросы
const { data } = useQuery(['groups'], fetchGroups, {
  staleTime: 5 * 60 * 1000 // 5 минут
});
```

### API запросы медленные

**Проверьте:**

1. **Индексы в БД:**
```sql
-- Создайте индексы на часто используемые поля
CREATE INDEX idx_attendance_lesson ON attendance(lesson_id);
CREATE INDEX idx_attendance_student ON attendance(student_id);
CREATE INDEX idx_attendance_date ON attendance(date);
```

2. **N+1 проблема:**
```javascript
// ❌ Плохо - N+1 запросов
for (const student of students) {
  const attendance = await getAttendance(student.id);
}

// ✅ Хорошо - один запрос
const attendance = await getAttendanceForStudents(studentIds);
```

3. **Пагинация:**
```javascript
GET /api/users?page=1&limit=50
```

### Высокая нагрузка на сервер

**Решения:**

1. **Rate limiting:**
```javascript
const rateLimit = require('express-rate-limit');
app.use('/api/', rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 100
}));
```

2. **Кэширование с Redis:**
```javascript
const redis = require('redis');
const client = redis.createClient();

// Кэшировать частые запросы
const cached = await client.get(`groups:${id}`);
if (cached) return JSON.parse(cached);
```

3. **CDN для статики:**
- Используйте Vercel/Netlify CDN
- CloudFlare для дополнительного кэширования

---

## 🔧 Troubleshooting

### "Network Error" в приложении

**Проверьте:**

1. **API URL:**
```env
# В max-webapp/.env
VITE_API_URL=https://your-api.com/api
```

2. **CORS:**
```javascript
// В server/server.js
app.use(cors({
  origin: process.env.ALLOWED_ORIGINS.split(',')
}));
```

3. **Сервер запущен:**
```bash
curl https://your-api.com/health
```

### База данных не подключается

**Решение:**
```bash
# Проверьте подключение
psql -h your-host -U your-user -d your-database

# Проверьте логи PostgreSQL
tail -f /var/log/postgresql/postgresql-14-main.log

# Проверьте firewall
sudo ufw status
```

### "Module not found" ошибка

**Решение:**
```bash
# Очистите node_modules
rm -rf node_modules package-lock.json

# Переустановите
npm install

# Проверьте версию Node.js
node --version  # должна быть >= 16.x
```

### Миграции БД не применяются

**Решение:**
```bash
# Пересоздайте таблицы
cd server
npm run init-db

# Или вручную через psql
psql -h host -U user -d database -f scripts/init-database.sql
```

### Не работает после обновления

**Решение:**
```bash
# Очистите кэш браузера
# В MAX: Settings → Data and Storage → Clear Cache

# Жёсткий перезапуск
# Закройте MAX полностью и откройте снова

# Проверьте версию API
curl https://your-api.com/
```

### Ошибки в production

**Где смотреть логи:**

**Vercel (frontend):**
- Dashboard → Deployments → Logs

**DigitalOcean (backend):**
```bash
# Через DO CLI
doctl apps logs <app-id>

# Или через SSH
pm2 logs max-attendance-api
```

**Heroku:**
```bash
heroku logs --tail --app your-app-name
```

---

## 🚀 Развёртывание

### Какой хостинг выбрать?

**Рекомендации:**

**Frontend:**
- ✅ **Vercel** - быстрый деплой, автоматический CI/CD
- ✅ **Netlify** - хорошая альтернатива
- GitHub Pages - для простых случаев

**Backend:**
- ✅ **DigitalOcean App Platform** - простота и гибкость
- ✅ **Railway** - современный и удобный
- Heroku - классический вариант
- AWS EC2 - максимальный контроль

### Сколько стоит хостинг?

**Минимальные расходы:**
- Frontend (Vercel): $0 (Hobby plan)
- Backend (Railway): $5/месяц
- Database (Railway): $5/месяц
- **Итого:** ~$10/месяц

**Для production:**
- Frontend (Vercel Pro): $20/месяц
- Backend (DO App Platform): $12/месяц
- Database (DO Managed): $15/месяц
- **Итого:** ~$47/месяц

### Нужен ли домен?

**Обязательно** для production:
- MAX требует HTTPS для WebApp
- Пользователи доверяют домену больше

**Где купить:**
- Namecheap: ~$10/год
- Cloudflare Registrar: по себестоимости
- Google Domains: ~$12/год

### Как настроить SSL?

**Автоматически (рекомендуется):**
- Vercel/Netlify - SSL включен автоматически
- Cloudflare - SSL + CDN бесплатно

**Вручную (Let's Encrypt):**
```bash
# Ubuntu/Debian
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

### Как откатить деплой?

**Vercel:**
```bash
vercel rollback <deployment-url>
```

**Railway:**
```bash
railway rollback
```

**Heroku:**
```bash
heroku releases
heroku rollback v42
```

### Можно ли запустить на shared hosting?

**Не рекомендуется** для Node.js приложений.

Требуется:
- VPS или dedicated сервер
- Или PaaS (Vercel, Railway, Heroku)

---

## 🆘 Получение помощи

### Где найти документацию?

- **README.md** - обзор проекта
- **DEPLOYMENT.md** - развёртывание
- **MAX_INTEGRATION_GUIDE.md** - интеграция с MAX
- **API_DOCUMENTATION.md** - документация API
- **SECURITY.md** - безопасность
- **FAQ.md** - этот документ

### Как сообщить о баге?

1. Проверьте существующие [Issues](https://github.com/yourusername/max-attendance/issues)
2. Создайте новый Issue с:
   - Описанием проблемы
   - Шагами воспроизведения
   - Ожидаемым и фактическим результатом
   - Версией приложения
   - Скриншотами (если применимо)

### Где задать вопрос?

- **GitHub Discussions** - для общих вопросов
- **GitHub Issues** - для багов и feature requests
- **Email:** support@yourdomain.com
- **MAX:** @MaxAttendanceSupportBot

### Как запросить новую функцию?

Создайте Feature Request в GitHub Issues с:
- Описанием функции
- Use case (зачем нужна)
- Ожидаемым поведением
- Примерами (если есть)

---

## 📊 Известные проблемы

### MAX WebApp не обновляется автоматически

**Workaround:**
```javascript
// Добавьте versioning
// main.jsx
const APP_VERSION = '1.0.0';
console.log('App version:', APP_VERSION);

// Проверяйте версию при загрузке
if (localStorage.getItem('app_version') !== APP_VERSION) {
  localStorage.clear();
  localStorage.setItem('app_version', APP_VERSION);
  window.location.reload();
}
```

### iOS Safari кэширует агрессивно

**Решение:**
```html
<!-- index.html -->
<meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
<meta http-equiv="Pragma" content="no-cache">
<meta http-equiv="Expires" content="0">
```

### Проблемы с timezone

**Решение:**
```javascript
// Всегда храните в UTC
const utcDate = new Date().toISOString();

// Конвертируйте в локальное время при отображении
import { format } from 'date-fns';
import { ru } from 'date-fns/locale';

const localDate = format(new Date(utcDate), 'PPP', { locale: ru });
```

---

## 🎓 Полезные команды

### Разработка

```bash
# Frontend dev server
cd max-webapp && npm run dev

# Backend dev server
cd server && npm run dev

# Линтинг
npm run lint

# Build
npm run build
```

### Бэкуп

```bash
# Бэкуп БД
pg_dump -h host -U user -d database > backup.sql

# Восстановление
psql -h host -U user -d database < backup.sql
```

### Production

```bash
# Проверка здоровья API
curl https://your-api.com/health

# Просмотр логов (PM2)
pm2 logs max-attendance-api

# Перезапуск
pm2 restart max-attendance-api

# Проверка процессов
pm2 status
```

---

**Не нашли ответ на свой вопрос?**

Свяжитесь с нами:
- 📧 Email: support@yourdomain.com
- 💬 MAX: @MaxAttendanceSupportBot
- 🐛 GitHub Issues: [Create Issue](https://github.com/yourusername/max-attendance/issues/new)

---

**Последнее обновление:** 2024-11-08