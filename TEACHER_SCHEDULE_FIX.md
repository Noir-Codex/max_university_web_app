# Решение проблемы с пустым расписанием преподавателя

## Проблема
В панели преподавателя не отображается расписание.

## Причина
В режиме разработки (dev mode) по умолчанию авторизуется пользователь **admin@university.ru** (роль: admin), у которого нет расписания, так как он не является преподавателем.

## Решение

### Способ 1: Переключение на преподавателя (рекомендуется)

1. **Откройте консоль браузера** (F12 → Console)

2. **Посмотрите доступных пользователей:**
   ```javascript
   showDevUsers()
   ```
   
   Вы увидите список:
   ```
   👥 Доступные пользователи для разработки:
     admin: admin@university.ru ← текущий
     teacher_ivanov: ivanov@university.ru
     teacher_petrov: petrov@university.ru
     teacher_sidorova: sidorova@university.ru
     student: smirnov@university.ru
   ```

3. **Переключитесь на преподавателя:**
   ```javascript
   switchDevUser(DEV_USERS.teacher_ivanov)
   ```
   
   Или напрямую:
   ```javascript
   switchDevUser("ivanov@university.ru")
   ```

4. **Страница автоматически перезагрузится** и вы будете авторизованы как преподаватель Иванов.

5. **Перейдите в "Расписание"** - теперь должны отображаться пары.

### Способ 2: Проверка текущего пользователя

В консоли браузера выполните:
```javascript
// Проверить текущего пользователя
localStorage.getItem('dev_user_email')

// Проверить данные авторизации
JSON.parse(localStorage.getItem('auth-storage'))
```

### Способ 3: Вернуться к админу

Если нужно вернуться к админу:
```javascript
switchDevUser(DEV_USERS.admin)
```

## Доступные преподаватели

| Email | Имя | Пароль |
|-------|-----|--------|
| ivanov@university.ru | Иван Иванов | admin123 |
| petrov@university.ru | Пётр Петров | admin123 |
| sidorova@university.ru | Анна Сидорова | admin123 |

## Проверка расписания

### В консоли браузера:

1. **Проверить, что загружается:**
   - Откройте DevTools (F12)
   - Перейдите в Console
   - Откройте панель преподавателя → Расписание
   - Смотрите логи:
     ```
     🔄 Загрузка расписания: { viewMode: "week", currentWeekType: 2, userId: 2 }
     📡 API запрос расписания: { teacher_id: 2 }
     📡 API ответ: 14 пар
     📅 Расписание загружено: 14 пар
     ```

2. **Если пар 0:**
   - Проверьте текущий тип недели (1 или 2)
   - Переключите неделю кнопкой в интерфейсе
   - Проверьте, что вы авторизованы как преподаватель (не админ)

### Через Network вкладку:

1. Откройте DevTools → Network
2. Обновите страницу расписания
3. Найдите запрос к `/api/schedule`
4. Проверьте:
   - **Request Headers:** должен быть `Authorization: Bearer <token>`
   - **Query Parameters:** `teacher_id`, `week_type`
   - **Response:** массив пар расписания

## Структура расписания

Расписание создано с учетом чередования недель:

- **week_type = 0** - пары, которые идут каждую неделю
- **week_type = 1** - пары на первой (нечетной) неделе
- **week_type = 2** - пары на второй (четной) неделе

Текущая неделя определяется автоматически от 1 сентября учебного года.

## Команды для быстрого доступа

Добавьте в консоль браузера:

```javascript
// Быстрое переключение
const switchToTeacher = () => switchDevUser(DEV_USERS.teacher_ivanov)
const switchToAdmin = () => switchDevUser(DEV_USERS.admin)
const switchToStudent = () => switchDevUser(DEV_USERS.student)

// Использование:
switchToTeacher()  // Переключиться на преподавателя
switchToAdmin()    // Переключиться на админа
```

## Если проблема сохраняется

1. **Очистите localStorage:**
   ```javascript
   localStorage.clear()
   location.reload()
   ```

2. **Пересоздайте тестовые данные:**
   ```bash
   cd server
   node scripts/seedData.js
   node scripts/seedScheduleWithWeekType.js
   ```

3. **Проверьте логи сервера:**
   - Должны быть запросы к `/api/schedule`
   - Не должно быть ошибок 401/403

4. **Проверьте, что серверы запущены:**
   - Frontend: http://localhost:3000
   - Backend: http://localhost:3001

## Примечание для продакшена

В продакшене авторизация будет происходить через MAX Bridge API автоматически, и пользователь будет определяться по данным из MAX мессенджера. Переключение пользователей доступно только в режиме разработки.

---

**Дата:** 9 ноября 2025  
**Статус:** Исправлено ✅



