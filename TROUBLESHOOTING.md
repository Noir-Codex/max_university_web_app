# 🔧 Решение проблем

## ⚠️ "Запрашиваемый ресурс не найден"

### Причина:
Браузер кеширует старую версию приложения или открыта старая вкладка.

### Решение:

#### **Способ 1: Жесткая перезагрузка (рекомендуется)**

1. **В браузере нажмите:**
   - **Windows/Linux:** `Ctrl + Shift + R` или `Ctrl + F5`
   - **Mac:** `Cmd + Shift + R`

2. Или через DevTools:
   - Откройте DevTools (F12)
   - Правый клик на кнопке обновления
   - Выберите "Очистить кеш и жесткая перезагрузка"

#### **Способ 2: Очистить кеш браузера**

1. Откройте DevTools (F12)
2. Перейдите в Application → Storage
3. Нажмите "Clear site data"
4. Обновите страницу (F5)

#### **Способ 3: Открыть в режиме инкогнито**

1. **Chrome/Edge:** `Ctrl + Shift + N` (Windows) или `Cmd + Shift + N` (Mac)
2. **Firefox:** `Ctrl + Shift + P` (Windows) или `Cmd + Shift + P` (Mac)
3. Откройте http://localhost:3000

#### **Способ 4: Перезапустить frontend сервер**

```bash
# Остановить
pkill -f "vite"

# Запустить заново
cd max-webapp
npm run dev
```

---

## 🔴 Сервер не запускается

### Проверка портов:

```bash
# Проверить, что порты свободны
lsof -i :3000  # Frontend
lsof -i :3001  # Backend

# Если порт занят - освободить
kill -9 <PID>
```

### Запуск серверов:

```bash
# Терминал 1 - Backend
cd server
npm run dev

# Терминал 2 - Frontend  
cd max-webapp
npm run dev
```

---

## 🔐 "Неверный email или пароль"

### Проверьте:

1. **Правильность email:**
   - `admin@university.ru` (не `admin@university.com`)
   - Без пробелов в начале/конце

2. **Правильность пароля:**
   - `admin123` (все строчные)
   - Проверьте раскладку клавиатуры
   - Убедитесь, что Caps Lock выключен

3. **Пользователь существует в БД:**
   ```bash
   cd server
   node -e "const db = require('./config/database'); db.query('SELECT email FROM users WHERE email = $1', ['admin@university.ru']).then(r => console.log(r.rows)).finally(() => process.exit())"
   ```

### Сброс пароля:

```bash
cd server
node scripts/resetPassword.js admin@university.ru admin123
```

---

## 🌐 Ошибка подключения к API

### Симптомы:
- "Network Error"
- "Failed to fetch"
- Долгая загрузка

### Решение:

1. **Проверить backend:**
   ```bash
   curl http://localhost:3001/health
   ```
   
   Должно вернуть: `{"status":"ok",...}`

2. **Проверить CORS:**
   - В файле `server/.env` должно быть:
   ```
   ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3003
   ```

3. **Перезапустить backend:**
   ```bash
   cd server
   pkill -f "node.*server"
   npm run dev
   ```

---

## 📊 База данных не отвечает

### Симптомы:
- "Connection timeout"
- "Cannot connect to database"

### Решение:

1. **Проверить PostgreSQL:**
   ```bash
   # Проверить статус
   brew services list | grep postgresql
   
   # Запустить если не запущен
   brew services start postgresql@14
   ```

2. **Проверить подключение:**
   ```bash
   cd server
   node -e "const db = require('./config/database'); db.query('SELECT NOW()').then(r => console.log('✅ БД работает:', r.rows[0])).catch(e => console.error('❌ Ошибка:', e.message)).finally(() => process.exit())"
   ```

3. **Пересоздать данные:**
   ```bash
   cd server
   node scripts/seedData.js
   node scripts/seedScheduleWithWeekType.js
   ```

---

## 🎨 Страница пустая / белый экран

### Решение:

1. **Откройте консоль браузера (F12)**
   - Посмотрите ошибки в Console
   - Проверьте Network на ошибки загрузки

2. **Очистите localStorage:**
   ```javascript
   // В консоли браузера
   localStorage.clear()
   location.reload()
   ```

3. **Проверьте, что React загружается:**
   ```javascript
   // В консоли браузера
   console.log(React)
   ```

---

## 🔄 Изменения не применяются

### Решение:

1. **Жесткая перезагрузка:** `Ctrl + Shift + R` (Windows) или `Cmd + Shift + R` (Mac)

2. **Очистить кеш Vite:**
   ```bash
   cd max-webapp
   rm -rf node_modules/.vite
   npm run dev
   ```

3. **Перезапустить оба сервера:**
   ```bash
   # Остановить все
   pkill -f "node"
   pkill -f "vite"
   
   # Запустить заново
   cd server && npm run dev &
   cd max-webapp && npm run dev &
   ```

---

## 📱 Проблемы на мобильном

### Решение:

1. **Очистить кеш приложения**
2. **Перезапустить MAX мессенджер**
3. **Проверить подключение к интернету**

---

## 🆘 Ничего не помогает

### Полный сброс:

```bash
# 1. Остановить все процессы
pkill -f "node"
pkill -f "vite"

# 2. Очистить кеши
cd max-webapp
rm -rf node_modules/.vite
rm -rf dist

# 3. Пересоздать БД
cd ../server
node scripts/seedData.js
node scripts/seedScheduleWithWeekType.js

# 4. Запустить серверы
npm run dev &
cd ../max-webapp
npm run dev
```

### Проверка после сброса:

1. Откройте **новую вкладку** в режиме инкогнито
2. Перейдите на http://localhost:3000
3. Должна открыться страница входа "MAX WebApp"
4. Войдите с `admin@university.ru / admin123`

---

## 📞 Контакты

Если проблема не решается - проверьте:
1. Логи backend в терминале
2. Консоль браузера (F12 → Console)
3. Network вкладку (F12 → Network)

Скопируйте ошибки и обратитесь к разработчику.

---

**Дата:** 9 ноября 2025



