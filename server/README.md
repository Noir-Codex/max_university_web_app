# MAX WebApp Backend API

Backend API сервер для системы управления посещаемостью MAX WebApp.

## 🚀 Технологии

- **Node.js** - серверная платформа
- **Express.js** - веб-фреймворк
- **PostgreSQL** - база данных
- **JWT** - аутентификация
- **Telegram WebApp** - интеграция с Telegram

## 📋 Требования

- Node.js >= 16.x
- PostgreSQL >= 13.x
- npm или yarn

## 🔧 Установка

### 1. Клонирование и установка зависимостей

```bash
cd server
npm install
```

### 2. Настройка переменных окружения

Скопируйте файл `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл с вашими настройками:

```env
# Сервер
PORT=3001
NODE_ENV=development

# База данных PostgreSQL
DB_USER=your_db_user
DB_HOST=your_db_host
DB_NAME=your_db_name
DB_PASSWORD=your_db_password
DB_PORT=5432

# Путь к SSL сертификату
DB_SSL_CERT_PATH=.cloud-certs/root.crt

# JWT секрет
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Telegram Bot Token
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# CORS
FRONTEND_URL=http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173,https://t.me

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Логирование
LOG_LEVEL=debug
```

### 3. Настройка SSL сертификата для PostgreSQL

Если ваша база данных требует SSL соединение, поместите сертификат `root.crt` в папку:

```
~/.cloud-certs/root.crt
```

Или укажите другой путь в переменной `DB_SSL_CERT_PATH`.

### 4. Инициализация базы данных

Выполните скрипт инициализации для создания всех таблиц:

```bash
npm run init-db
```

Это создаст следующие таблицы:
- `users` - пользователи системы
- `groups` - учебные группы
- `group_students` - связь студентов с группами
- `subjects` - учебные дисциплины
- `schedule` - расписание занятий
- `attendance` - посещаемость студентов

## 🎯 Запуск

### Режим разработки (с hot-reload)

```bash
npm run dev
```

### Режим production

```bash
npm start
```

Сервер будет доступен по адресу: `http://localhost:3001`

## 📡 API Endpoints

### Аутентификация

- `POST /api/auth/login` - Вход через Telegram WebApp
- `GET /api/auth/me` - Получить текущего пользователя
- `POST /api/auth/logout` - Выход

### Пользователи

- `GET /api/users` - Получить всех пользователей (Admin)
- `GET /api/users/:id` - Получить пользователя по ID
- `POST /api/users` - Создать пользователя (Admin)
- `PUT /api/users/:id` - Обновить пользователя (Admin)
- `DELETE /api/users/:id` - Удалить пользователя (Admin)
- `GET /api/users/teachers` - Получить список преподавателей
- `GET /api/users/students/:groupId` - Получить студентов группы

### Группы

- `GET /api/groups` - Получить все группы
- `GET /api/groups/:id` - Получить группу по ID
- `POST /api/groups` - Создать группу (Admin)
- `PUT /api/groups/:id` - Обновить группу (Admin)
- `DELETE /api/groups/:id` - Удалить группу (Admin)
- `GET /api/groups/:id/students` - Получить студентов группы
- `POST /api/groups/:id/students` - Добавить студента в группу (Admin)
- `DELETE /api/groups/:id/students/:studentId` - Удалить студента из группы (Admin)

### Расписание

- `GET /api/schedule` - Получить расписание с фильтрами
- `GET /api/schedule/:id` - Получить пару по ID
- `GET /api/schedule/today` - Получить расписание на сегодня
- `POST /api/schedule` - Создать пару (Admin)
- `PUT /api/schedule/:id` - Обновить пару (Admin)
- `DELETE /api/schedule/:id` - Удалить пару (Admin)
- `GET /api/schedule/stats` - Получить статистику расписания (Admin)

### Посещаемость

- `GET /api/attendance` - Получить посещаемость с фильтрами
- `GET /api/attendance/lesson/:lessonId` - Получить посещаемость для пары
- `POST /api/attendance` - Сохранить посещаемость (Teacher/Admin)
- `POST /api/attendance/bulk` - Массовое сохранение посещаемости (Teacher/Admin)
- `PUT /api/attendance/:id` - Обновить посещаемость (Teacher/Admin)
- `DELETE /api/attendance/:id` - Удалить посещаемость (Teacher/Admin)
- `GET /api/attendance/stats/student/:studentId` - Статистика студента
- `GET /api/attendance/stats/group/:groupId` - Статистика группы
- `GET /api/attendance/stats/subject/:subjectId` - Статистика по дисциплине
- `GET /api/attendance/stats/overall` - Общая статистика (Admin)

### Дисциплины

- `GET /api/subjects` - Получить все дисциплины
- `GET /api/subjects/:id` - Получить дисциплину по ID
- `POST /api/subjects` - Создать дисциплину (Admin)
- `PUT /api/subjects/:id` - Обновить дисциплину (Admin)
- `DELETE /api/subjects/:id` - Удалить дисциплину (Admin)
- `GET /api/subjects/teacher/:teacherId` - Дисциплины преподавателя

### Отчеты

- `GET /api/reports/attendance` - Получить отчет о посещаемости
- `GET /api/reports/export` - Экспортировать отчет (CSV/XLSX)
- `GET /api/reports/stats/groups` - Статистика по группам (Admin)
- `GET /api/reports/stats/subjects` - Статистика по дисциплинам (Admin)
- `GET /api/reports/stats/overall` - Общая статистика системы (Admin)
- `GET /api/reports/export/groups` - Экспорт статистики групп (Admin)

### Импорт

- `POST /api/import/schedule/validate` - Валидировать данные перед импортом (Admin)
- `POST /api/import/schedule` - Импортировать расписание из файла (Admin)

### Служебные

- `GET /health` - Проверка состояния сервера
- `GET /` - Информация об API

## 🔐 Аутентификация

API использует JWT токены для аутентификации. Все защищенные endpoints требуют заголовок:

```
Authorization: Bearer <token>
```

### Роли пользователей

- **admin** - полный доступ ко всем функциям
- **teacher** - управление посещаемостью и просмотр своих групп
- **student** - просмотр собственной посещаемости

## 📊 Структура базы данных

```
users (пользователи)
├── id (PK)
├── telegram_id (UNIQUE)
├── username
├── first_name
├── last_name
├── role
├── email (UNIQUE)
└── password_hash

groups (группы)
├── id (PK)
├── name (UNIQUE)
├── course
├── specialty
└── curator_id (FK -> users)

group_students (связь студентов и групп)
├── id (PK)
├── group_id (FK -> groups)
└── student_id (FK -> users)

subjects (дисциплины)
├── id (PK)
├── name (UNIQUE)
├── type
└── hours

schedule (расписание)
├── id (PK)
├── subject_id (FK -> subjects)
├── group_id (FK -> groups)
├── teacher_id (FK -> users)
├── day_of_week
├── time_start
├── time_end
├── room
├── week_number
└── lesson_type

attendance (посещаемость)
├── id (PK)
├── lesson_id (FK -> schedule)
├── student_id (FK -> users)
├── date
├── status
└── notes
```

## 🛠 Разработка

### Структура проекта

```
server/
├── config/          # Конфигурация (БД и т.д.)
├── controllers/     # Контроллеры для обработки запросов
├── middleware/      # Middleware (аутентификация, валидация и т.д.)
├── models/          # Модели данных
├── routes/          # Определение маршрутов API
├── scripts/         # Скрипты инициализации
├── utils/           # Утилиты (валидация, экспорт и т.д.)
├── .env             # Переменные окружения (не в git)
├── .env.example     # Пример переменных окружения
├── package.json     # Зависимости проекта
├── server.js        # Точка входа
└── README.md        # Документация
```

### Скрипты npm

- `npm start` - Запуск в production режиме
- `npm run dev` - Запуск в development режиме с nodemon
- `npm run init-db` - Инициализация базы данных

## 🐛 Отладка

### Проверка подключения к БД

```bash
# Через psql
psql -h your_host -U your_user -d your_database

# Или проверить health endpoint
curl http://localhost:3001/health
```

### Логи

В режиме разработки (`NODE_ENV=development`) все запросы логируются в консоль.

В production режиме используется формат `combined`.

## 🔒 Безопасность

- JWT токены для аутентификации
- Хеширование паролей с bcrypt
- Валидация всех входящих данных
- Защита от SQL инъекций (параметризованные запросы)
- Rate limiting для защиты от DDoS
- CORS настроен для разрешенных доменов
- Helmet для защиты заголовков HTTP

## 📝 Примечания

- Все даты хранятся в UTC
- Время указывается в формате HH:MM (24 часа)
- День недели: 1 = Понедельник, 7 = Воскресенье
- Статусы посещаемости: `present`, `absent`, `late`, `excused`

## 🤝 Поддержка

При возникновении проблем проверьте:
1. Правильность настроек в `.env`
2. Доступность базы данных PostgreSQL
3. Наличие SSL сертификата (если требуется)
4. Логи сервера для диагностики ошибок

## 📄 Лицензия

ISC